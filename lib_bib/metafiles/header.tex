%%% TEXT FORMATTING ===================================================== %
% Character Encoding
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}

% Language and Font
\usepackage{polyglossia}
  \setmainlanguage{english}
  \setotherlanguage[variant=ancient]{greek}
  %\newfontfamily\greekfont[Script=Greek]{GFS Baskerville}
  \newfontfamily\greekfont[Script=Greek]{Brill}
  
% Incorrect forms
\newcommand{\iform}[2][G]{%
  \ifx G#1 \hspace{-1ex}\bgroup\sffamily\textenglish{\textsuperscript{\tiny x}}\egroup\textgreek{#2}\else
  \ifx L#1 \hspace{-1ex}\bgroup\sffamily\textenglish{\textsuperscript{\tiny x}}\egroup\textenglish{#2}%
  \fi\fi
}
			% Well hot damn it works

% Philological Characters
\usepackage{teubner}

% Smart Quote Formatting
\usepackage{csquotes}

% Mathematical Characters
\usepackage{amsmath}
  \let\emptyset=\varnothing
  
% Fractions!
\usepackage{units} % Contains nicefrac
%\usepackage{l3packages} % Contains xfrac (for \sfrac{}{})
  \usepackage{xfrac}


% IPA Characters
\usepackage[safe]{tipa}
  \newcommand{\sub}[1]{\textsubscript{#1}}
  \newcommand\K{\kern.05em} % small amount of kerning
  \newcommand\KK{\kern.1em} % middle amount of kerning
  \newcommand\KKK{\kern.2em} % big amount of kerning
  \newcommand\WK{\kern-.05em} % Wario Kerning

% Code Formatting
%\usepackage{minted}

% Header Formatting
\usepackage{sectsty}
  \chapterfont{\centering\normalfont\nohang\textsc} % H1
  
\usepackage{titlesec}

% Reference Formatting
\usepackage{hyperref}

% Dummy Text
\usepackage{lipsum}

% Microtype
\usepackage{microtype}
  \microtypecontext{spacing=nonfrench}

%%% DOCUMENT FORMATTING ================================================= %
% To-Do Notes for Editing
\usepackage[colorinlistoftodos]{todonotes}
  % Defined with xcolor
  \definecolor{npink}{RGB}{245,169,184} % Notes pink
  \definecolor{nblue}{RGB}{91,206,250}

% Incude Subdocuments
\usepackage{subfiles}

% Include Appendices
\usepackage[toc]{appendix}

% Line Spacing
\usepackage{setspace}
  %\setstretch{1.1} % Not Double-Spaced
  \doublespacing   % Double-Spaced

% Floating sep.
\textfloatsep 0.75in

% Disallow widow and orphan lines
\usepackage[all]{nowidow}

% Disallow unwanted spaces between paragraphs
\raggedbottom

%%% FIGURE FORMATTING =================================================== %
% TikZ & PGF
\usepackage{pgfkeys}

% Pictures
\usepackage{graphicx}

% Tables
% Enforce Captions Above
% Note from the stack exchange where the DigiLab got this:
%   "A drawback of this is that you can't have more than one
%   caption per table, i.e. you can't have two different
%   tabulars with two captions side-by-side"
\usepackage{float}
  \floatstyle{plaintop}
  \restylefloat{table}

% booktabs!
\usepackage{booktabs}

%%% BIBLIOGRAPHY FORMATTING ============================================= %
%\usepackage{biblatex-chicago}
%\usepackage[backend=biber,
%            autocites=footnote]{biblatex-chicago}
  %\addbibresource{metafiles/thesis.bib}
  %\addbibresource{subfiles/references.bib}

%%% BOOLEANS ============================================================ %
\newif\ifsidenote
\newif\iffigures
\newif\iftables

%%% Testing a Fix for Teubner =========================================== %
% Omg it works?
% I was getting a consistent error at compliation

% Undefined control sequence.
% \brevis  ->\textormath 
%                        {{{\metricsfont b}}}{\br@ve }
% l.896 ...icron and eta would form an iamb, \brevis

% So I redefined \textormath to match \TextOrMath in the base LaTeX release

% Looking through the Teubner doc, I think this should also fix \;

\let\textormath\TextOrMath

% Hopefully fixes tables without changing paper justification 
\usepackage[raggedrightboxes]{ragged2e}

% Eat Pages
\pagenumbering{gobble}